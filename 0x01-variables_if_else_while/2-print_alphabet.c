#include <stdio.h>
/**
 * main - This is the entry point
 *
 * Return: ) if succesful
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
