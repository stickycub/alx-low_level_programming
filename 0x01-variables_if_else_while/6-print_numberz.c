#include <stdio.h>
/**
 * main - This is the entrry point
 *
 * Return: 0 if succesful
 */

int main(void)
{
	int a;

	a = 0;
	while
		(a < 10) {
			putchar(a + '0');
			a++;
		}
	putchar('\n');
	return (0);
}
