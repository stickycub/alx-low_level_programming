#include <stdio.h>
/**
 * main - This is the entry point
 *
 * Return: 0 if succesful
 */

int main(void)
{
	int c;

	c = 'z';
		while
			(c <= 'z') {
				putchar(c);
				c--;
			}
	putchar('\n');
	return (0);
}
